# Google Gemini API Key
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Customize the model name (default: gemini-1.5-flash-latest)
# GEMINI_MODEL=gemini-1.5-flash-latest

# Optional: Adjust temperature for LLM responses (default: 0.7)
# GEMINI_TEMPERATURE=0.7

# Optional: Set ChromaDB persistence directory (default: ./chroma_db)
# CHROMA_PERSIST_DIR=./chroma_db

# Optional: Set documents directory (default: ./documents)
# DOCUMENTS_DIR=./documents 